export default function inputItem (task) {
    const container = document.createElement("div");
    const textfield = document.createElement("input");
    textfield.value = task;
    input.type = "text";
    textfield.appendChild(container);
return container();
}
